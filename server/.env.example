# ClawChat 服务端配置示例
# 复制此文件为 .env 并按需修改

# 服务端口（默认 3900）
PORT=3900

# 监听地址（0.0.0.0 = 所有网卡，127.0.0.1 = 仅本机）
HOST=0.0.0.0

# JWT 密钥（留空则自动从 ~/.openclaw/openclaw.json 读取 gateway.auth.token）
# JWT_SECRET=your-secret-key-here

# OpenClaw 网关端口（留空则自动从 openclaw.json 读取，默认 18789）
# GATEWAY_PORT=18789
